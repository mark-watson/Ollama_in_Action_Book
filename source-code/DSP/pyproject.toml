[project]
name = "dsp-tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.0.3",
    "ollama>=0.6.1",
]

[tool.uv]
exclude-newer = "2 days"
